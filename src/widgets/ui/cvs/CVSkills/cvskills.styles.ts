import styled from 'styled-components';

export const CVSkillsContainer = styled.div`
  margin-top: 2rem;
  display: grid;
  gap: 1rem;
  padding: 1rem;
  background-color: ${({ theme }) => theme.lightGrey};
  border-radius: 0.5rem;
`;
